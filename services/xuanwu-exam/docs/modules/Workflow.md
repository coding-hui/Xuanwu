# 模块：发布与审批（Workflow）

## 目标
- 管理草稿/待审/已发布/归档的状态流转；支持审批流程。

## 范围
- 对试卷与考试生效；记录审批意见与结果。

## 领域模型
- 状态枚举与审批记录实体（按需设计）。

## API 端点
- `/papers/{id}/publish`、`/papers/{id}/approve`。
- `/exams/{id}/publish`、`/exams/{id}/approve`。

## 测试建议
- 状态机与权限校验；审批与发布的并发控制与审计记录。

